import React from 'react'
import PropTypes from 'prop-types'

function SelectColors(props) {
  const {
    highlightedIndex,
    selectedIndexes,
    getOptionProps,
    getContainerProps = () => {},
  } = useListbox(labelId, options);
  return (
    <div>
      
    </div>
  )
}

SelectColors.propTypes = {

}

export default SelectColors

